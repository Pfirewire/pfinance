import styled from "styled-components";


function LoginForm() {
    return(
        <LoginFormWrapper>
            Login Form
        </LoginFormWrapper>
    );
}

export default LoginForm;

const LoginFormWrapper = styled.form`
  
`;